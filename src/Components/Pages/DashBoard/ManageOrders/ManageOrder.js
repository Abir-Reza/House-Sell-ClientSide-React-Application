import React from 'react';

const ManageOrder = ({order}) => {
    const {clientName, houseTitle, housePlace, houseCountry} = order;
    console.log(clientName);
    return (
        <div>
        </div>
    );
};

export default ManageOrder;